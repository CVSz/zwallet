{
  "name": "zwallet-backend",
  "private": true,
  "workspaces": ["packages/*", "services/*"],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev",
    "lint": "npm run -ws lint"
  }
}
